import { ProfileTabs } from '@/feautures/profile';
import { ProfileOrderDetailSection } from '@/sections-pages/profile';
import React from 'react';

const ProfileOrderDetail = () => {
  return (
    <ProfileTabs>
      <ProfileOrderDetailSection />
    </ProfileTabs>
  );
};

export default ProfileOrderDetail;
